
from position import Position
from piece import Piece

class Bishop(Piece):


    def symbol(self) -> str:
        return "B"

    def isValidMove(self, newPosition: Position, board) -> bool:
        # --- TODO ---
        # col_delta = abs(col_index_new - col_index_current)
        # row_delta = abs(newPosition.row - self._position.row)
        # Valid if col_delta == row_delta (and > 0)
        # Use board.isPathClear() to check for blocking pieces.
        # Destination must not be a same-color piece.
        return True   # stub

if __name__ == "__main__":
    b = Bishop(Position("c", 1), 0)
    print(f"Bishop created: {b}, image: {b.image_filename()}")
    assert b.symbol() == "B"
    assert b.image_filename() == "Bw.png"
    print("Bishop functional test passed!")
